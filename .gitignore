# Ignore all directories, and all sub-directories, and it's contents :
/*

# Include the src directory, and all it's sub-directories, and it's contents :
!/src
!/src/*

# Include the gradle directory, and all it's sub-directories, and it's contents :
!/gradle
!/gradle/*

# Include the .gitignore, build.gradle, gradlew, gradlew.bat files :
!.gitignore
!build.gradle
!gradlew
!gradlew.bat